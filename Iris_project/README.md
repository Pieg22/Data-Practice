# First data enviorament . 

Instalamos Anaconda en WSL (¡superando los errores!).

Configuramos un entorno conda profesional fijando la versión de Python.

Instalamos todas las librerías clave de ciencia de datos.

Verificamos el entorno en JupyterLab.

Exploramos el dataset 'iris' (EDA), creando tablas y gráficos (pairplot, heatmap).

Entrenamos un modelo de Machine Learning (DecisionTree) y obtuvimos un 100% de precisión.

Documentamos el proyecto con un README.txt y un environment.yml.
